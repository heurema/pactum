schema: pactum.config.v1
map:
  max_file_bytes: 500000
  code_index: auto
gate:
  scope_enforcement: block
execute:
  models: []
review:
  max_rounds: 10
  patience: 2
  clean_rounds: 1
  budget:
    mode: warn
    max_tokens: null
  panel:
    - agent: claude
      model: claude-fable-5
    - agent: codex

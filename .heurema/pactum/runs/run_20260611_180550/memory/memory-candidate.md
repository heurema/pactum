# Memory Candidate

## Run
- Run id: run_20260611_180550
- Source: deterministic

## Contract
- Goal: Fix three valid external review findings. (1) pactum export must preserve its no-overwrite guarantee at rename time: today two concurrent exports targeting the same --output can both pass the early existence check and the final os.Rename silently replaces the file that appeared in the window; finalize through a no-replace path (e.g. os.Link from the staged temp file to the output then remove the temp, treating link EEXIST as the existing 'output path already exists' error; keep behavior correct on Windows where os.Rename already refuses to replace) so a concurrent winner is never clobbered. (2) pactum export must reject run-record relative paths containing backslashes before writing ZIP entry names: on Unix a backslash is a valid filename character and filepath.ToSlash does not rewrite it, which would emit a non-portable entry name that some Windows extractors treat as a separator; fail the export with a clear error naming the offending path. (3) memory accept must not accept a stale candidate: memory propose records a freshness pin of the review state (for example the review document's updated_at or a content hash) inside memory-candidate.json; memory accept verifies the pin against the current review document and fails with a clear error plus error.fix pactum memory propose <run_id> when the review changed after the candidate was generated; the next-affordance selection that advertises memory accept applies the same staleness check and advertises memory propose instead when stale. Tests cover: concurrent-window overwrite refusal (simulate by creating the output file between staging and finalize), backslash rejection, stale-candidate refusal with the fix affordance, and the next selector switching to propose on staleness.
- In scope:
  - Finalize `pactum export` archives through a no-replace path so an output file created after staging but before finalization is never overwritten.
  - Validate final ZIP entry names after `filepath.ToSlash` and reject any entry name that still contains a literal backslash, reporting the offending run-relative path.
  - Add a versioned freshness pin to `memory-candidate.json` generated by `pactum memory propose` using a deterministic content hash over `review/review.json`, `review/findings.jsonl`, `review/resolutions.jsonl`, `review/proposals.jsonl`, and `review/proposal-decisions.jsonl`.
  - Make `pactum memory accept` verify the candidate freshness pin before appending memory, updating acceptance, updating project memory, or writing ledger events.
  - Update next-affordance selection so stale memory candidates do not advertise `pactum memory accept`.
- Out of scope:
  - Changing the contract goal or clarification answers.
  - Changing the successful export archive layout, deterministic metadata, or filtered-events sidecar behavior except where needed for backslash validation.
  - Accepting legacy memory candidates that lack a recognized freshness pin.
  - Running real agent execution commands such as `pactum execute run` or `pactum review run`.
- Acceptance criteria:
  - If an export output path appears after ZIP staging and before finalization, export fails with the existing clear `output path already exists` style error, preserves the existing file contents, and removes the staged temporary archive.
  - On Unix-like platforms, export finalization uses a no-replace mechanism such as linking the staged archive to the output path and treating `EEXIST` as an existing-output failure; on Windows, behavior remains no-clobber where `os.Rename` already refuses to replace an existing destination.
  - Export rejects a run-record path whose final archive entry name contains `\` after `filepath.ToSlash`, fails before producing the requested archive, names the offending run-relative path in the error, and does not reject normal Windows separators that are converted to `/`.
  - `pactum memory propose` writes a freshness pin into `memory-candidate.json`; the pin is stable for unchanged full review state and changes when any of the five pinned review-state artifacts changes.
  - `pactum memory accept` treats a missing, unrecognized, or mismatched freshness pin as stale or unverifiable, exits without appending a memory item or mutating acceptance/project-memory/ledger state, and emits a clear stale-candidate error.
  - When a stale candidate's current review state is still approved and proposal-clean, `memory accept` exposes `error.fix: pactum memory propose <run_id>`.
  - When a stale candidate's current review state is no longer approved or proposal-clean, `memory accept` refuses acceptance and exposes `next: ["pactum review show <run_id>"]` rather than pointing at an illegal memory propose command.
  - For a stale `memory_proposed` run, lifecycle next-affordance selection advertises `pactum memory propose <run_id>` when reproposal is legal, or `pactum review show <run_id>` when review preconditions must be repaired; it does not advertise `pactum memory accept <run_id>`.
  - Fresh memory candidates generated from the current full review state still accept successfully and preserve existing memory acceptance behavior.
- Validation commands:
  - go test ./internal/app -run 'Test(Export|Memory|Lifecycle)'
  - make check

## Outcome
- Gate status: needs_review
- Review status: approved
- Execution exit code: 0
- Validation passed: true
- Changes need review: true

## Changes
- Changed files:
  - internal/app/affordances_test.go
  - internal/app/errors.go
  - internal/app/export.go
  - internal/app/export_test.go
  - internal/app/memory.go
  - internal/app/memory_freshness_test.go
  - internal/app/memory_test.go
  - internal/app/resolve.go
  - internal/app/status.go
  - internal/app/task.go
- New files: none
- Missing files: none

## Clarifications
- q_001: For the memory-staleness finding, should "review state" mean only `review/review.json` and its `updated_at`, or the full review state assembled by `buildReviewStateWithProposals` from `review/review.json`, `review/findings.jsonl`, `review/resolutions.jsonl`, `review/proposals.jsonl`, and `review/proposal-decisions.jsonl`?
  Answer: Define the freshness pin as a deterministic content hash over the full review state used to build the memory candidate: `review/review.json`, `review/findings.jsonl`, `review/resolutions.jsonl`, `review/proposals.jsonl`, and `review/proposal-decisions.jsonl`; `memory accept` and next-affordance selection compare that pin to the current full-state hash.
- q_002: How should `pactum memory accept` handle an existing `memory/memory-candidate.json` that was generated before this change and therefore has no freshness pin?
  Answer: Treat a missing or unrecognized freshness pin as stale/unverifiable: do not append a memory item, fail with a clear stale-candidate error, and direct the user to regenerate with `pactum memory propose <run_id>`.
- q_003: For export path validation, should "run-record relative paths containing backslashes" reject only literal `\` characters that remain in the ZIP entry name after platform separator conversion, rather than rejecting normal Windows path separators?
  Answer: Validate the final archive entry name after `filepath.ToSlash`; reject any entry name that still contains `\`, and report the offending run-relative path. This rejects Unix filenames with literal backslashes while preserving normal Windows separator conversion.
- q_004: If a memory candidate is stale and the current review state also makes `pactum memory propose <run_id>` illegal, for example a new blocking finding reset review approval or a pending proposal was added, should `memory accept` still return `error.fix: pactum memory propose <run_id>` or should it point to review inspection first?
  Answer: Use a context-sensitive affordance: if the current review state is still approved and proposal-clean, stale `memory accept` fails with `error.fix: pactum memory propose <run_id>`; if review approval or proposal preconditions are no longer satisfied, fail without accepting and expose `next: ["pactum review show <run_id>"]` so the user can repair the review state first.

## Review Decisions
- f_001 [high] resolved internal/app/export_windows.go:15: Windows finalize does not provide the no-clobber guarantee: os.Rename on Windows replaces an existing destination file (documented os.Rename semantics since Go 1.5, implemented via MoveFileEx with MOVEFILE_REPLACE_EXISTING). The comment claiming rename 'refuses to replace an existing destination' is false, so the concurrent-window overwrite this contract fixes remains open on Windows, failing acceptance criterion 2. The new test TestExportFinalizeRefusesToReplaceOutputCreatedAfterStaging (internal/app/export_test.go:224) has no Windows skip and would fail on Windows; CI is ubuntu-only so it is never exercised. A no-replace Windows finalize needs MoveFileEx without the replace flag (golang.org/x/sys/windows) or an O_CREATE|O_EXCL create-and-copy.
  Resolution: Removed the os.Rename-based Windows finalize and its false no-replace claim. Finalize is now a single cross-platform os.Link at the call site in internal/app/export.go (writeExportArchive); link fails with ERROR_ALREADY_EXISTS on Windows, which os.IsExist maps to the existing 'output path already exists' error, so the concurrent-window no-clobber guarantee holds on Windows and TestExportFinalizeRefusesToReplaceOutputCreatedAfterStaging would pass on GOOS=windows.
- f_002 [low] resolved internal/app/memory.go:365: Freshness pin is computed from a second read of the five review artifacts (memory.go:365) after the candidate content was built from earlier reads (memory.go:329-344). A review mutation landing between the reads yields a candidate with pre-mutation content and a post-mutation pin; memory accept then verifies the pin and accepts a candidate that does not match the review state it embeds. Hashing the same bytes parsed for candidate construction would close the window.
- f_003 [low] resolved internal/app/export_unix.go:24: Hard-link finalize fails on filesystems without hard-link support (exFAT, FAT32, some SMB/NFS mounts): os.Link returns EPERM/ENOTSUP, so exporting to e.g. an exFAT external drive — which previously succeeded via rename — now always fails, and with a raw LinkError rather than a clear message since only EEXIST is mapped at export.go:322. An O_CREATE|O_EXCL create-and-copy fallback would preserve the no-replace guarantee on such destinations.
- f_004 [medium] resolved internal/app/export_windows.go:15: Windows finalize does not preserve no-clobber: Go's os.Rename replaces an existing destination on Windows (documented stdlib behavior since Go 1.5, golang/go#8914, MoveFileEx with MOVEFILE_REPLACE_EXISTING; repo pins go 1.26). The new comment asserts the opposite, acceptance criterion #2's Windows clause is unmet, and TestExportFinalizeRefusesToReplaceOutputCreatedAfterStaging would fail on GOOS=windows — unobserved because CI is ubuntu-only and never compiles or runs the Windows path. Minimal fix: use os.Link on Windows too (NTFS hard links fail with ERROR_ALREADY_EXISTS, recognized by os.IsExist).
  Resolution: Applied this finding's proposed minimal fix: os.Link on Windows too. Since both platforms then had identical implementations, the platform-specific finalizeExportArchive was deleted from export_unix.go and export_windows.go and the link inlined at the single call site in export.go, with the comment corrected to state that os.Rename replaces an existing destination on Windows.
- f_005 [medium] resolved internal/app/resolve.go:382: nextCommandForStatus still returns 'pactum memory accept' unconditionally for memory_proposed, with no staleness check. It surfaces as NextCommand in task show (printed to humans under a 'Next:' header), task list, and status, so a stale-candidate run still advertises memory accept on those surfaces despite the in-scope item 'stale memory candidates do not advertise pactum memory accept'. The JSON next affordance (nextCommandsForRun) is correctly fixed and tested; accept itself refuses stale candidates safely with corrective affordances, and this selector is coarse by pre-existing design for other states (e.g. review_approved ignores pending proposals), so this is advisory.
- f_006 [high] resolved internal/app/export_windows.go:15: Windows finalize path is never compiled or executed by any validation (gate ran on darwin, CI is ubuntu-only, make check has no GOOS=windows build), and the guarantee it encodes is false: Go documents os.Rename as replacing an existing non-directory newpath, implemented on Windows via MoveFileEx(MOVEFILE_REPLACE_EXISTING) since Go 1.5. TestExportFinalizeRefusesToReplaceOutputCreatedAfterStaging would fail if run on Windows but never runs there, so the suite reads green on acceptance criterion 2 while the Windows no-clobber behavior it asserts is unmet.
  Resolution: The unvalidated Windows-only finalize path no longer exists: finalize is shared code in export.go, compiled and exercised by TestExportFinalizeRefusesToReplaceOutputCreatedAfterStaging on every platform, so the suite no longer reads green on a guarantee it never tests. A GOOS=windows cross-vet in make check was attempted and reverted as infeasible: internal/app transitively depends on CGO-only tree-sitter bindings whose build constraints exclude all Go files under cross-compilation, so no package containing the windows file can be cross-vetted without a Windows C toolchain.
- f_007 [low] resolved internal/app/export.go:166: The directory call site of the new backslash validation (entry names built as archiveRoot+rel+"/") has no test; TestExportRejectsBackslashInEntryName covers only a regular file with a backslash, leaving the dir branch of collectExportEntries unexercised.
- f_008 [low] resolved internal/app/export.go:282: Successful exports now rely solely on the deferred os.Remove to clean up the staged archive (link-based finalize leaves the source name behind, unlike the old rename), and no success-path test asserts absence of .pactum-export-* leftovers; all assertNoExportLeftovers calls are in failure tests, so dropping the defer would pass the full suite.
- f_009 [medium] resolved docs/memory.md:31: docs/memory.md 'Propose, show, accept' (and the memory summary in docs/flow.md:284-289) does not document the new freshness-pin behavior: memory propose now writes a freshness_pin into memory-candidate.json, and memory accept verifies it and refuses stale candidates with a memory_candidate_stale error plus a context-sensitive fix (pactum memory propose <run_id>) or next (pactum review show <run_id>) affordance. In particular, candidates proposed before this change have no recognized pin and are now unconditionally rejected until re-proposed — a user-visible behavior break the docs should state.
- f_010 [low] resolved docs/flow.md:304: docs/flow.md still documents export finalization as 'created via a temporary sibling file and an atomic rename', but the change replaced rename with a platform no-replace finalize: os.Link plus staged-file cleanup on Unix, rename only on Windows. The documented mechanism no longer matches the implementation or the strengthened no-clobber guarantee.
- f_011 [low] resolved docs/flow.md:305: docs/flow.md's enumerated export failure conditions omit the new backslash rejection: export now fails — before producing the archive — when a run-record path's entry name still contains a literal backslash after filepath.ToSlash, naming the offending run-relative path. The failure list (output exists, parent missing, path inside run dir, unreadable file, malformed ledger, non-regular entries) is now incomplete.
- f_012 [medium] resolved internal/app/resolve.go:381: Stale memory candidates are still advertised through the legacy next_command path. nextCommandsForRun is freshness-aware, but nextCommandForStatus returns `pactum memory accept` for every `memory_proposed` run, and task/status outputs still expose that value.
  Resolution: nextCommandForStatus (internal/app/resolve.go) now takes (paths, runID, status) and applies the same staleness gate as nextCommandsForRun for memory_proposed: accept only when memoryCandidateFresh, propose when memoryReproposalLegal, review show otherwise. Updated all four call sites (task list, task show, both status branches) and added TestNextCommandSwitchesToProposeForStaleMemoryCandidate pinning the legacy next_command field across fresh, stale-but-reproposable, and pending-proposal states.
- f_013 [low] resolved internal/app/memory_test.go:478: Missing test for unrecognized freshness-pin schemas. The contract requires missing, unrecognized, and mismatched freshness pins to be rejected, but the test only deletes the pin entirely; it does not cover a wrong schema with an otherwise valid hash.
- f_014 [low] resolved docs/flow.md:304: The export workflow docs still say the archive is created via an atomic rename and omit the new failure case for run-record paths whose ZIP entry name contains a literal backslash.
- f_015 [low] resolved docs/memory.md:31: The memory workflow docs do not mention that memory candidates are pinned to the current review state and that accept rejects stale or unpinned candidates until the user reruns memory propose.
- f_016 [low] resolved no file: Empirical staleness probe
- Proposal summary: pending=0 accepted=15 rejected=0

## Reusable Project Knowledge
- scope: in scope: Finalize `pactum export` archives through a no-replace path so an output file created after staging but before finalization is never overwritten.
- scope: in scope: Validate final ZIP entry names after `filepath.ToSlash` and reject any entry name that still contains a literal backslash, reporting the offending run-relative path.
- scope: in scope: Add a versioned freshness pin to `memory-candidate.json` generated by `pactum memory propose` using a deterministic content hash over `review/review.json`, `review/findings.jsonl`, `review/resolutions.jsonl`, `review/proposals.jsonl`, and `review/proposal-decisions.jsonl`.
- scope: in scope: Make `pactum memory accept` verify the candidate freshness pin before appending memory, updating acceptance, updating project memory, or writing ledger events.
- scope: in scope: Update next-affordance selection so stale memory candidates do not advertise `pactum memory accept`.
- scope: out of scope: Changing the contract goal or clarification answers.
- scope: out of scope: Changing the successful export archive layout, deterministic metadata, or filtered-events sidecar behavior except where needed for backslash validation.
- scope: out of scope: Accepting legacy memory candidates that lack a recognized freshness pin.
- scope: out of scope: Running real agent execution commands such as `pactum execute run` or `pactum review run`.
- clarification: q_001: For the memory-staleness finding, should "review state" mean only `review/review.json` and its `updated_at`, or the full review state assembled by `buildReviewStateWithProposals` from `review/review.json`, `review/findings.jsonl`, `review/resolutions.jsonl`, `review/proposals.jsonl`, and `review/proposal-decisions.jsonl`? Answer: Define the freshness pin as a deterministic content hash over the full review state used to build the memory candidate: `review/review.json`, `review/findings.jsonl`, `review/resolutions.jsonl`, `review/proposals.jsonl`, and `review/proposal-decisions.jsonl`; `memory accept` and next-affordance selection compare that pin to the current full-state hash.
- clarification: q_002: How should `pactum memory accept` handle an existing `memory/memory-candidate.json` that was generated before this change and therefore has no freshness pin? Answer: Treat a missing or unrecognized freshness pin as stale/unverifiable: do not append a memory item, fail with a clear stale-candidate error, and direct the user to regenerate with `pactum memory propose <run_id>`.
- clarification: q_003: For export path validation, should "run-record relative paths containing backslashes" reject only literal `\` characters that remain in the ZIP entry name after platform separator conversion, rather than rejecting normal Windows path separators? Answer: Validate the final archive entry name after `filepath.ToSlash`; reject any entry name that still contains `\`, and report the offending run-relative path. This rejects Unix filenames with literal backslashes while preserving normal Windows separator conversion.
- clarification: q_004: If a memory candidate is stale and the current review state also makes `pactum memory propose <run_id>` illegal, for example a new blocking finding reset review approval or a pending proposal was added, should `memory accept` still return `error.fix: pactum memory propose <run_id>` or should it point to review inspection first? Answer: Use a context-sensitive affordance: if the current review state is still approved and proposal-clean, stale `memory accept` fails with `error.fix: pactum memory propose <run_id>`; if review approval or proposal preconditions are no longer satisfied, fail without accepting and expose `next: ["pactum review show <run_id>"]` so the user can repair the review state first.
- review_resolution: f_001 resolved: Windows finalize does not provide the no-clobber guarantee: os.Rename on Windows replaces an existing destination file (documented os.Rename semantics since Go 1.5, implemented via MoveFileEx with MOVEFILE_REPLACE_EXISTING). The comment claiming rename 'refuses to replace an existing destination' is false, so the concurrent-window overwrite this contract fixes remains open on Windows, failing acceptance criterion 2. The new test TestExportFinalizeRefusesToReplaceOutputCreatedAfterStaging (internal/app/export_test.go:224) has no Windows skip and would fail on Windows; CI is ubuntu-only so it is never exercised. A no-replace Windows finalize needs MoveFileEx without the replace flag (golang.org/x/sys/windows) or an O_CREATE|O_EXCL create-and-copy.; resolution: Removed the os.Rename-based Windows finalize and its false no-replace claim. Finalize is now a single cross-platform os.Link at the call site in internal/app/export.go (writeExportArchive); link fails with ERROR_ALREADY_EXISTS on Windows, which os.IsExist maps to the existing 'output path already exists' error, so the concurrent-window no-clobber guarantee holds on Windows and TestExportFinalizeRefusesToReplaceOutputCreatedAfterStaging would pass on GOOS=windows.
- review_resolution: f_002 resolved: Freshness pin is computed from a second read of the five review artifacts (memory.go:365) after the candidate content was built from earlier reads (memory.go:329-344). A review mutation landing between the reads yields a candidate with pre-mutation content and a post-mutation pin; memory accept then verifies the pin and accepts a candidate that does not match the review state it embeds. Hashing the same bytes parsed for candidate construction would close the window.
- review_resolution: f_003 resolved: Hard-link finalize fails on filesystems without hard-link support (exFAT, FAT32, some SMB/NFS mounts): os.Link returns EPERM/ENOTSUP, so exporting to e.g. an exFAT external drive — which previously succeeded via rename — now always fails, and with a raw LinkError rather than a clear message since only EEXIST is mapped at export.go:322. An O_CREATE|O_EXCL create-and-copy fallback would preserve the no-replace guarantee on such destinations.
- review_resolution: f_004 resolved: Windows finalize does not preserve no-clobber: Go's os.Rename replaces an existing destination on Windows (documented stdlib behavior since Go 1.5, golang/go#8914, MoveFileEx with MOVEFILE_REPLACE_EXISTING; repo pins go 1.26). The new comment asserts the opposite, acceptance criterion #2's Windows clause is unmet, and TestExportFinalizeRefusesToReplaceOutputCreatedAfterStaging would fail on GOOS=windows — unobserved because CI is ubuntu-only and never compiles or runs the Windows path. Minimal fix: use os.Link on Windows too (NTFS hard links fail with ERROR_ALREADY_EXISTS, recognized by os.IsExist).; resolution: Applied this finding's proposed minimal fix: os.Link on Windows too. Since both platforms then had identical implementations, the platform-specific finalizeExportArchive was deleted from export_unix.go and export_windows.go and the link inlined at the single call site in export.go, with the comment corrected to state that os.Rename replaces an existing destination on Windows.
- review_resolution: f_005 resolved: nextCommandForStatus still returns 'pactum memory accept' unconditionally for memory_proposed, with no staleness check. It surfaces as NextCommand in task show (printed to humans under a 'Next:' header), task list, and status, so a stale-candidate run still advertises memory accept on those surfaces despite the in-scope item 'stale memory candidates do not advertise pactum memory accept'. The JSON next affordance (nextCommandsForRun) is correctly fixed and tested; accept itself refuses stale candidates safely with corrective affordances, and this selector is coarse by pre-existing design for other states (e.g. review_approved ignores pending proposals), so this is advisory.
- review_resolution: f_006 resolved: Windows finalize path is never compiled or executed by any validation (gate ran on darwin, CI is ubuntu-only, make check has no GOOS=windows build), and the guarantee it encodes is false: Go documents os.Rename as replacing an existing non-directory newpath, implemented on Windows via MoveFileEx(MOVEFILE_REPLACE_EXISTING) since Go 1.5. TestExportFinalizeRefusesToReplaceOutputCreatedAfterStaging would fail if run on Windows but never runs there, so the suite reads green on acceptance criterion 2 while the Windows no-clobber behavior it asserts is unmet.; resolution: The unvalidated Windows-only finalize path no longer exists: finalize is shared code in export.go, compiled and exercised by TestExportFinalizeRefusesToReplaceOutputCreatedAfterStaging on every platform, so the suite no longer reads green on a guarantee it never tests. A GOOS=windows cross-vet in make check was attempted and reverted as infeasible: internal/app transitively depends on CGO-only tree-sitter bindings whose build constraints exclude all Go files under cross-compilation, so no package containing the windows file can be cross-vetted without a Windows C toolchain.
- review_resolution: f_007 resolved: The directory call site of the new backslash validation (entry names built as archiveRoot+rel+"/") has no test; TestExportRejectsBackslashInEntryName covers only a regular file with a backslash, leaving the dir branch of collectExportEntries unexercised.
- review_resolution: f_008 resolved: Successful exports now rely solely on the deferred os.Remove to clean up the staged archive (link-based finalize leaves the source name behind, unlike the old rename), and no success-path test asserts absence of .pactum-export-* leftovers; all assertNoExportLeftovers calls are in failure tests, so dropping the defer would pass the full suite.
- review_resolution: f_009 resolved: docs/memory.md 'Propose, show, accept' (and the memory summary in docs/flow.md:284-289) does not document the new freshness-pin behavior: memory propose now writes a freshness_pin into memory-candidate.json, and memory accept verifies it and refuses stale candidates with a memory_candidate_stale error plus a context-sensitive fix (pactum memory propose <run_id>) or next (pactum review show <run_id>) affordance. In particular, candidates proposed before this change have no recognized pin and are now unconditionally rejected until re-proposed — a user-visible behavior break the docs should state.
- review_resolution: f_010 resolved: docs/flow.md still documents export finalization as 'created via a temporary sibling file and an atomic rename', but the change replaced rename with a platform no-replace finalize: os.Link plus staged-file cleanup on Unix, rename only on Windows. The documented mechanism no longer matches the implementation or the strengthened no-clobber guarantee.
- review_resolution: f_011 resolved: docs/flow.md's enumerated export failure conditions omit the new backslash rejection: export now fails — before producing the archive — when a run-record path's entry name still contains a literal backslash after filepath.ToSlash, naming the offending run-relative path. The failure list (output exists, parent missing, path inside run dir, unreadable file, malformed ledger, non-regular entries) is now incomplete.
- review_resolution: f_012 resolved: Stale memory candidates are still advertised through the legacy next_command path. nextCommandsForRun is freshness-aware, but nextCommandForStatus returns `pactum memory accept` for every `memory_proposed` run, and task/status outputs still expose that value.; resolution: nextCommandForStatus (internal/app/resolve.go) now takes (paths, runID, status) and applies the same staleness gate as nextCommandsForRun for memory_proposed: accept only when memoryCandidateFresh, propose when memoryReproposalLegal, review show otherwise. Updated all four call sites (task list, task show, both status branches) and added TestNextCommandSwitchesToProposeForStaleMemoryCandidate pinning the legacy next_command field across fresh, stale-but-reproposable, and pending-proposal states.
- review_resolution: f_013 resolved: Missing test for unrecognized freshness-pin schemas. The contract requires missing, unrecognized, and mismatched freshness pins to be rejected, but the test only deletes the pin entirely; it does not cover a wrong schema with an otherwise valid hash.
- review_resolution: f_014 resolved: The export workflow docs still say the archive is created via an atomic rename and omit the new failure case for run-record paths whose ZIP entry name contains a literal backslash.
- review_resolution: f_015 resolved: The memory workflow docs do not mention that memory candidates are pinned to the current review state and that accept rejects stale or unpinned candidates until the user reruns memory propose.
- review_resolution: f_016 resolved: Empirical staleness probe
- review_resolution: proposal p_001 accepted as f_001
- review_resolution: proposal p_002 accepted as f_002
- review_resolution: proposal p_003 accepted as f_003
- review_resolution: proposal p_004 accepted as f_004
- review_resolution: proposal p_005 accepted as f_005
- review_resolution: proposal p_006 accepted as f_006
- review_resolution: proposal p_007 accepted as f_007
- review_resolution: proposal p_008 accepted as f_008
- review_resolution: proposal p_009 accepted as f_009
- review_resolution: proposal p_010 accepted as f_010
- review_resolution: proposal p_011 accepted as f_011
- review_resolution: proposal p_012 accepted as f_012
- review_resolution: proposal p_013 accepted as f_013
- review_resolution: proposal p_014 accepted as f_014
- review_resolution: proposal p_015 accepted as f_015
- validation: go test ./internal/app -run 'Test(Export|Memory|Lifecycle)' passed
- validation: make check passed

## Artifacts
- Contract: contract/contract.json
- Gate report: gate/gate-report.json
- Review: review/review.json
- Findings: review/findings.jsonl
- Resolutions: review/resolutions.jsonl
- Proposals: review/proposals.jsonl
- Proposal decisions: review/proposal-decisions.jsonl

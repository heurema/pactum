# Review Fixer Context

## Run
- Run id: run_20260611_180550
- Run status: contract_approved

## Approved contract
- Goal: Fix three valid external review findings. (1) pactum export must preserve its no-overwrite guarantee at rename time: today two concurrent exports targeting the same --output can both pass the early existence check and the final os.Rename silently replaces the file that appeared in the window; finalize through a no-replace path (e.g. os.Link from the staged temp file to the output then remove the temp, treating link EEXIST as the existing 'output path already exists' error; keep behavior correct on Windows where os.Rename already refuses to replace) so a concurrent winner is never clobbered. (2) pactum export must reject run-record relative paths containing backslashes before writing ZIP entry names: on Unix a backslash is a valid filename character and filepath.ToSlash does not rewrite it, which would emit a non-portable entry name that some Windows extractors treat as a separator; fail the export with a clear error naming the offending path. (3) memory accept must not accept a stale candidate: memory propose records a freshness pin of the review state (for example the review document's updated_at or a content hash) inside memory-candidate.json; memory accept verifies the pin against the current review document and fails with a clear error plus error.fix pactum memory propose <run_id> when the review changed after the candidate was generated; the next-affordance selection that advertises memory accept applies the same staleness check and advertises memory propose instead when stale. Tests cover: concurrent-window overwrite refusal (simulate by creating the output file between staging and finalize), backslash rejection, stale-candidate refusal with the fix affordance, and the next selector switching to propose on staleness.
- In scope:
  - Finalize `pactum export` archives through a no-replace path so an output file created after staging but before finalization is never overwritten.
  - Validate final ZIP entry names after `filepath.ToSlash` and reject any entry name that still contains a literal backslash, reporting the offending run-relative path.
  - Add a versioned freshness pin to `memory-candidate.json` generated by `pactum memory propose` using a deterministic content hash over `review/review.json`, `review/findings.jsonl`, `review/resolutions.jsonl`, `review/proposals.jsonl`, and `review/proposal-decisions.jsonl`.
  - Make `pactum memory accept` verify the candidate freshness pin before appending memory, updating acceptance, updating project memory, or writing ledger events.
  - Update next-affordance selection so stale memory candidates do not advertise `pactum memory accept`.
- Out of scope:
  - Changing the contract goal or clarification answers.
  - Changing the successful export archive layout, deterministic metadata, or filtered-events sidecar behavior except where needed for backslash validation.
  - Accepting legacy memory candidates that lack a recognized freshness pin.
  - Running real agent execution commands such as `pactum execute run` or `pactum review run`.
- Acceptance criteria:
  - If an export output path appears after ZIP staging and before finalization, export fails with the existing clear `output path already exists` style error, preserves the existing file contents, and removes the staged temporary archive.
  - On Unix-like platforms, export finalization uses a no-replace mechanism such as linking the staged archive to the output path and treating `EEXIST` as an existing-output failure; on Windows, behavior remains no-clobber where `os.Rename` already refuses to replace an existing destination.
  - Export rejects a run-record path whose final archive entry name contains `\` after `filepath.ToSlash`, fails before producing the requested archive, names the offending run-relative path in the error, and does not reject normal Windows separators that are converted to `/`.
  - `pactum memory propose` writes a freshness pin into `memory-candidate.json`; the pin is stable for unchanged full review state and changes when any of the five pinned review-state artifacts changes.
  - `pactum memory accept` treats a missing, unrecognized, or mismatched freshness pin as stale or unverifiable, exits without appending a memory item or mutating acceptance/project-memory/ledger state, and emits a clear stale-candidate error.
  - When a stale candidate's current review state is still approved and proposal-clean, `memory accept` exposes `error.fix: pactum memory propose <run_id>`.
  - When a stale candidate's current review state is no longer approved or proposal-clean, `memory accept` refuses acceptance and exposes `next: ["pactum review show <run_id>"]` rather than pointing at an illegal memory propose command.
  - For a stale `memory_proposed` run, lifecycle next-affordance selection advertises `pactum memory propose <run_id>` when reproposal is legal, or `pactum review show <run_id>` when review preconditions must be repaired; it does not advertise `pactum memory accept <run_id>`.
  - Fresh memory candidates generated from the current full review state still accept successfully and preserve existing memory acceptance behavior.
- Validation commands:
  - go test ./internal/app -run 'Test(Export|Memory|Lifecycle)'
  - make check

## Current review findings
- Summary: findings=15 open=15 resolved=0 blocking_open=4
- Blocking findings (fix or rebut these — emit exactly one fix-outcome for each):
  - f_001 severity=high category=correctness blocking=true status=open: Windows finalize does not provide the no-clobber guarantee: os.Rename on Windows replaces an existing destination file (documented os.Rename semantics since Go 1.5, implemented via MoveFileEx with MOVEFILE_REPLACE_EXISTING). The comment claiming rename 'refuses to replace an existing destination' is false, so the concurrent-window overwrite this contract fixes remains open on Windows, failing acceptance criterion 2. The new test TestExportFinalizeRefusesToReplaceOutputCreatedAfterStaging (internal/app/export_test.go:224) has no Windows skip and would fail on Windows; CI is ubuntu-only so it is never exercised. A no-replace Windows finalize needs MoveFileEx without the replace flag (golang.org/x/sys/windows) or an O_CREATE|O_EXCL create-and-copy.
    location: internal/app/export_windows.go:15
  - f_004 severity=medium category=correctness blocking=true status=open: Windows finalize does not preserve no-clobber: Go's os.Rename replaces an existing destination on Windows (documented stdlib behavior since Go 1.5, golang/go#8914, MoveFileEx with MOVEFILE_REPLACE_EXISTING; repo pins go 1.26). The new comment asserts the opposite, acceptance criterion #2's Windows clause is unmet, and TestExportFinalizeRefusesToReplaceOutputCreatedAfterStaging would fail on GOOS=windows — unobserved because CI is ubuntu-only and never compiles or runs the Windows path. Minimal fix: use os.Link on Windows too (NTFS hard links fail with ERROR_ALREADY_EXISTS, recognized by os.IsExist).
    location: internal/app/export_windows.go:15
  - f_006 severity=high category=validation blocking=true status=open: Windows finalize path is never compiled or executed by any validation (gate ran on darwin, CI is ubuntu-only, make check has no GOOS=windows build), and the guarantee it encodes is false: Go documents os.Rename as replacing an existing non-directory newpath, implemented on Windows via MoveFileEx(MOVEFILE_REPLACE_EXISTING) since Go 1.5. TestExportFinalizeRefusesToReplaceOutputCreatedAfterStaging would fail if run on Windows but never runs there, so the suite reads green on acceptance criterion 2 while the Windows no-clobber behavior it asserts is unmet.
    location: internal/app/export_windows.go:15
  - f_012 severity=medium category=correctness blocking=true status=open: Stale memory candidates are still advertised through the legacy next_command path. nextCommandsForRun is freshness-aware, but nextCommandForStatus returns `pactum memory accept` for every `memory_proposed` run, and task/status outputs still expose that value.
    location: internal/app/resolve.go:381
- Advisory (non-blocking) findings (context only — do NOT edit code and do NOT emit outcomes for them):
  - f_002 severity=low category=correctness blocking=false status=open: Freshness pin is computed from a second read of the five review artifacts (memory.go:365) after the candidate content was built from earlier reads (memory.go:329-344). A review mutation landing between the reads yields a candidate with pre-mutation content and a post-mutation pin; memory accept then verifies the pin and accepts a candidate that does not match the review state it embeds. Hashing the same bytes parsed for candidate construction would close the window.
    location: internal/app/memory.go:365
  - f_003 severity=low category=correctness blocking=false status=open: Hard-link finalize fails on filesystems without hard-link support (exFAT, FAT32, some SMB/NFS mounts): os.Link returns EPERM/ENOTSUP, so exporting to e.g. an exFAT external drive — which previously succeeded via rename — now always fails, and with a raw LinkError rather than a clear message since only EEXIST is mapped at export.go:322. An O_CREATE|O_EXCL create-and-copy fallback would preserve the no-replace guarantee on such destinations.
    location: internal/app/export_unix.go:24
  - f_005 severity=medium category=scope blocking=false status=open: nextCommandForStatus still returns 'pactum memory accept' unconditionally for memory_proposed, with no staleness check. It surfaces as NextCommand in task show (printed to humans under a 'Next:' header), task list, and status, so a stale-candidate run still advertises memory accept on those surfaces despite the in-scope item 'stale memory candidates do not advertise pactum memory accept'. The JSON next affordance (nextCommandsForRun) is correctly fixed and tested; accept itself refuses stale candidates safely with corrective affordances, and this selector is coarse by pre-existing design for other states (e.g. review_approved ignores pending proposals), so this is advisory.
    location: internal/app/resolve.go:382
  - f_007 severity=low category=quality blocking=false status=open: The directory call site of the new backslash validation (entry names built as archiveRoot+rel+"/") has no test; TestExportRejectsBackslashInEntryName covers only a regular file with a backslash, leaving the dir branch of collectExportEntries unexercised.
    location: internal/app/export.go:166
  - f_008 severity=low category=quality blocking=false status=open: Successful exports now rely solely on the deferred os.Remove to clean up the staged archive (link-based finalize leaves the source name behind, unlike the old rename), and no success-path test asserts absence of .pactum-export-* leftovers; all assertNoExportLeftovers calls are in failure tests, so dropping the defer would pass the full suite.
    location: internal/app/export.go:282
  - f_009 severity=medium category=quality blocking=false status=open: docs/memory.md 'Propose, show, accept' (and the memory summary in docs/flow.md:284-289) does not document the new freshness-pin behavior: memory propose now writes a freshness_pin into memory-candidate.json, and memory accept verifies it and refuses stale candidates with a memory_candidate_stale error plus a context-sensitive fix (pactum memory propose <run_id>) or next (pactum review show <run_id>) affordance. In particular, candidates proposed before this change have no recognized pin and are now unconditionally rejected until re-proposed — a user-visible behavior break the docs should state.
    location: docs/memory.md:31
  - f_010 severity=low category=quality blocking=false status=open: docs/flow.md still documents export finalization as 'created via a temporary sibling file and an atomic rename', but the change replaced rename with a platform no-replace finalize: os.Link plus staged-file cleanup on Unix, rename only on Windows. The documented mechanism no longer matches the implementation or the strengthened no-clobber guarantee.
    location: docs/flow.md:304
  - f_011 severity=low category=quality blocking=false status=open: docs/flow.md's enumerated export failure conditions omit the new backslash rejection: export now fails — before producing the archive — when a run-record path's entry name still contains a literal backslash after filepath.ToSlash, naming the offending run-relative path. The failure list (output exists, parent missing, path inside run dir, unreadable file, malformed ledger, non-regular entries) is now incomplete.
    location: docs/flow.md:305
  - f_013 severity=low category=quality blocking=false status=open: Missing test for unrecognized freshness-pin schemas. The contract requires missing, unrecognized, and mismatched freshness pins to be rejected, but the test only deletes the pin entirely; it does not cover a wrong schema with an otherwise valid hash.
    location: internal/app/memory_test.go:478
  - f_014 severity=low category=quality blocking=false status=open: The export workflow docs still say the archive is created via an atomic rename and omit the new failure case for run-record paths whose ZIP entry name contains a literal backslash.
    location: docs/flow.md:304
  - f_015 severity=low category=quality blocking=false status=open: The memory workflow docs do not mention that memory candidates are pinned to the current review state and that accept rejects stale or unpinned candidates until the user reruns memory propose.
    location: docs/memory.md:31

## Artifacts
- Contract: contract/contract.json
- Review: review/review.json
- Findings: review/findings.jsonl
- Resolutions: review/resolutions.jsonl
- Gate report: gate/gate-report.json
- Execution result: execute/last-result.json

## Fixer guidance
- Source files are the source of truth.
- Use `pactum search "<term>"` and inspect current source files before relying on this context.
- For each current review finding, trace the finding to the code.
- If a finding is valid, fix it in place within the approved contract scope.
- If a finding is a false positive, leave code unchanged for that finding and explain the rebuttal in your final output.
- Do not approve the review or mutate review findings/resolutions/proposals.
- Do not modify generated `.heurema` artifacts.

{
  "schema": "pactum.contract.v1",
  "run_id": "run_20260610_220413",
  "status": "draft",
  "goal": "Add an export command that dumps a run's full record as a single archive",
  "scope": {
    "in": [],
    "out": []
  },
  "acceptance_criteria": [],
  "validation": {
    "commands": []
  },
  "assumptions": [],
  "open_questions": [
    "What should 'full record' include for export: only `.heurema/pactum/runs/\u003crun_id\u003e/`, that run directory plus filtered workspace-level ledger events, generated map/context artifacts, raw `.log` transcripts, or accepted project memory outside the run?",
    "Where should the export command live and what CLI shape should it expose: a top-level `pactum export`, a `pactum task export`, or another command group?",
    "What archive format and internal path layout should 'single archive' mean: `.zip`, `.tar.gz`, or another format, and should paths be rooted at the run id or copied exactly relative to `.heurema/pactum/`?",
    "Should exporting mutate Pactum state, such as appending an `export_*` event to the ledger or updating `run.json`, or should it be read-only apart from writing the archive file?",
    "For the concrete scenario where the requested output archive already exists, should `pactum export` overwrite it, fail, or require a force flag?",
    "For the concrete scenario where the output path is inside `.heurema/pactum/runs/\u003crun_id\u003e/`, should export reject it to avoid including the archive inside itself?",
    "For the concrete scenario where a run is still in progress or a file disappears while export is reading it, should export allow a partial snapshot, wait/lock, or fail?",
    "What acceptance checks should define completion for this export feature?",
    "For the concrete scenario where `--output reports/run.zip` is requested but the `reports/` parent directory does not exist, should export create the parent directories or fail?",
    "For the concrete scenario where the same run is exported twice with identical file contents but different filesystem mtimes or permissions, should the archive be byte-for-byte deterministic or preserve source file metadata?",
    "For the concrete scenario where `.heurema/pactum/ledger/events.jsonl` is missing, unreadable, or contains a malformed JSONL line while building the filtered run events sidecar, should export fail or produce the archive without those events?"
  ],
  "clarifications": {
    "questions": [
      {
        "id": "q_001",
        "question": "What should 'full record' include for export: only `.heurema/pactum/runs/\u003crun_id\u003e/`, that run directory plus filtered workspace-level ledger events, generated map/context artifacts, raw `.log` transcripts, or accepted project memory outside the run?",
        "blocking": true,
        "kind": "terminology",
        "rationale": "The repo has run-local paths in `contractRunPaths`, but lifecycle events are written to `.heurema/pactum/ledger/events.jsonl`. The actual workspace `.gitignore` ignores generated `runs/*/context/` and `*.log`, while structured execute/review/ledger artifacts are versionable. This changes archive contents, size, and privacy.",
        "recommended_answer": "Define 'full record' as all files currently under `.heurema/pactum/runs/\u003crun_id\u003e/`, including raw `.log` files if present, plus a generated export-only filtered copy of `.heurema/pactum/ledger/events.jsonl` containing only events for that run. Exclude project map, cache, tmp, locks, and global accepted memory unless the file already exists inside the run directory.",
        "confidence": "medium",
        "status": "open"
      },
      {
        "id": "q_002",
        "question": "Where should the export command live and what CLI shape should it expose: a top-level `pactum export`, a `pactum task export`, or another command group?",
        "blocking": true,
        "kind": "scope",
        "rationale": "The existing CLI has top-level groups for lifecycle stages and no `run` group. The goal says 'Add an export command' but does not name the command path, arguments, or output flag.",
        "recommended_answer": "Add a top-level read-oriented command `pactum export [run_id] --output \u003cpath\u003e [--json]`. Resolve omitted `run_id` using the same current-run/sole-active behavior as other read-only run commands.",
        "confidence": "medium",
        "status": "open"
      },
      {
        "id": "q_003",
        "question": "What archive format and internal path layout should 'single archive' mean: `.zip`, `.tar.gz`, or another format, and should paths be rooted at the run id or copied exactly relative to `.heurema/pactum/`?",
        "blocking": true,
        "kind": "terminology",
        "rationale": "The repository currently ignores `.zip` and `.tar` in project-map scans but has no archive implementation. Format and path layout affect cross-platform behavior and tests.",
        "recommended_answer": "Produce a ZIP archive with deterministic, slash-separated relative entries rooted at `pactum-run-\u003crun_id\u003e/`, with stable sorted file order and no absolute local paths introduced by the exporter.",
        "confidence": "medium",
        "depends_on": [
          "q_001"
        ],
        "status": "open",
        "blocked": true
      },
      {
        "id": "q_004",
        "question": "Should exporting mutate Pactum state, such as appending an `export_*` event to the ledger or updating `run.json`, or should it be read-only apart from writing the archive file?",
        "blocking": true,
        "kind": "assumption",
        "rationale": "Existing read-only commands do not append ledger events, but export creates an external artifact. Mutating state would make export part of the audited run record; read-only export keeps repeated exports from changing the record being exported.",
        "recommended_answer": "Treat export as read-only Pactum state: do not update `run.json`, contract files, approval, or ledgers. The only write is the requested archive output path, and JSON/human output reports what was exported.",
        "confidence": "high",
        "depends_on": [
          "q_002"
        ],
        "status": "open",
        "blocked": true
      },
      {
        "id": "q_005",
        "question": "For the concrete scenario where the requested output archive already exists, should `pactum export` overwrite it, fail, or require a force flag?",
        "blocking": true,
        "kind": "edge_case",
        "rationale": "Archive creation is a filesystem write outside normal Pactum state. Overwriting could destroy a prior export, while adding `--force` expands scope.",
        "recommended_answer": "Fail if the output path already exists. Do not add `--force` in this change unless explicitly requested later.",
        "confidence": "high",
        "depends_on": [
          "q_002"
        ],
        "status": "open",
        "blocked": true
      },
      {
        "id": "q_006",
        "question": "For the concrete scenario where the output path is inside `.heurema/pactum/runs/\u003crun_id\u003e/`, should export reject it to avoid including the archive inside itself?",
        "blocking": true,
        "kind": "edge_case",
        "rationale": "If the archive is created inside the directory being walked, the export can recursively include itself or produce nondeterministic contents.",
        "recommended_answer": "Reject output paths inside the exported run directory. Write via a temporary sibling file and atomically rename on success when possible.",
        "confidence": "high",
        "depends_on": [
          "q_002",
          "q_003"
        ],
        "status": "open",
        "blocked": true
      },
      {
        "id": "q_007",
        "question": "For the concrete scenario where a run is still in progress or a file disappears while export is reading it, should export allow a partial snapshot, wait/lock, or fail?",
        "blocking": true,
        "kind": "edge_case",
        "rationale": "Runs can be active and agent/review attempts may write files over time. The repo has lock directories but no current export semantics.",
        "recommended_answer": "Allow exporting any existing run status as a best-effort current on-disk snapshot, but fail the command if any file selected for export cannot be read during archive creation; remove the partial archive on failure.",
        "confidence": "medium",
        "depends_on": [
          "q_001",
          "q_004"
        ],
        "status": "open",
        "blocked": true
      },
      {
        "id": "q_008",
        "question": "What acceptance checks should define completion for this export feature?",
        "blocking": false,
        "kind": "acceptance",
        "rationale": "The contract draft has no acceptance criteria or validation commands. The repo standard requires `make check` before reporting changes.",
        "recommended_answer": "Acceptance requires tests proving `pactum export [run_id] --output \u003cpath\u003e` creates a ZIP containing the expected run files and filtered run events, supports `--json`, fails for missing runs, fails when output exists, rejects output inside the exported run directory, and leaves Pactum run/ledger state unchanged. Validation command: `make check`.",
        "confidence": "high",
        "depends_on": [
          "q_001",
          "q_002",
          "q_003",
          "q_004",
          "q_005",
          "q_006",
          "q_007"
        ],
        "status": "open",
        "blocked": true
      },
      {
        "id": "q_009",
        "question": "For the concrete scenario where `.heurema/pactum/runs/\u003crun_id\u003e/` contains a symlink or non-regular filesystem entry such as a FIFO, socket, or device file, should `pactum export` follow it, preserve it, skip it, or fail?",
        "blocking": true,
        "kind": "edge_case",
        "rationale": "The current questions define archive contents broadly, but the repo does not define archive behavior for symlinks or special files. Following a symlink could leak files outside the run record, while preserving such entries in ZIP is platform-sensitive.",
        "recommended_answer": "Export only regular files and directories. Do not follow symlinks, and fail the export if any symlink or non-regular filesystem entry is encountered under the selected run record.",
        "confidence": "high",
        "status": "answered",
        "answer": "Export only regular files and directories. Do not follow symlinks, and fail the export if any symlink or non-regular filesystem entry is encountered under the selected run record."
      },
      {
        "id": "q_010",
        "question": "How should `--output \u003cpath\u003e` be interpreted when the user passes a relative path from a subdirectory: relative to the invocation working directory, relative to the repository root, or relative to `.heurema/pactum/`?",
        "blocking": false,
        "kind": "scope",
        "rationale": "The existing command-shape question names `--output \u003cpath\u003e` but does not specify path resolution. Pactum has repo-relative path globs for contract scope, while normal file output flags usually resolve relative to the process working directory.",
        "recommended_answer": "Accept absolute output paths. Resolve relative output paths against the process working directory where `pactum export` was invoked, and allow output outside the repository except for the explicit rejection of paths inside the exported run directory.",
        "confidence": "high",
        "status": "answered",
        "answer": "Accept absolute output paths. Resolve relative output paths against the process working directory where `pactum export` was invoked, and allow output outside the repository except for the explicit rejection of paths inside the exported run directory."
      },
      {
        "id": "q_011",
        "question": "For the concrete scenario where `--output reports/run.zip` is requested but the `reports/` parent directory does not exist, should export create the parent directories or fail?",
        "blocking": false,
        "kind": "edge_case",
        "rationale": "Pactum's internal store creates parent directories for workspace artifacts, but this command writes a user-requested external artifact. Auto-creating arbitrary external directories could hide path typos.",
        "recommended_answer": "Fail if the output path's parent directory does not already exist. The command may create only its temporary archive file in that existing directory before atomically renaming it to the requested output path.",
        "confidence": "medium",
        "status": "open"
      },
      {
        "id": "q_012",
        "question": "For the concrete scenario where the same run is exported twice with identical file contents but different filesystem mtimes or permissions, should the archive be byte-for-byte deterministic or preserve source file metadata?",
        "blocking": false,
        "kind": "edge_case",
        "rationale": "The existing archive-format question recommends a deterministic ZIP with sorted entries, but the repo does not define whether ZIP entry metadata should be normalized. Pactum generally favors deterministic artifacts, and preserving mtimes or host-specific modes would make repeated exports differ even when the run record contents are unchanged.",
        "recommended_answer": "Normalize archive entry metadata instead of preserving host-specific source metadata: use stable slash-separated names, fixed ZIP timestamps, regular file mode 0644, directory mode 0755, and no owner/group or absolute path metadata. Acceptance may check stable entry order and byte-stable output for unchanged inputs.",
        "confidence": "medium",
        "status": "open"
      },
      {
        "id": "q_013",
        "question": "For the concrete scenario where `.heurema/pactum/ledger/events.jsonl` is missing, unreadable, or contains a malformed JSONL line while building the filtered run events sidecar, should export fail or produce the archive without those events?",
        "blocking": false,
        "kind": "edge_case",
        "rationale": "The current questions cover unreadable selected files, but filtering workspace ledger events requires reading and parsing a file outside the run directory. The event ledger is part of Pactum's audit trail, so silently omitting it would make a 'full record' export incomplete.",
        "recommended_answer": "If filtered workspace events are part of the export, fail the export when `events.jsonl` is missing, unreadable, or malformed, and remove any partial archive. If the ledger is readable but has no events for the run, include an empty filtered `events.jsonl` sidecar.",
        "confidence": "medium",
        "status": "open"
      },
      {
        "id": "q_014",
        "question": "What exact success payload should `pactum export --json` emit after creating an archive?",
        "blocking": false,
        "kind": "acceptance",
        "rationale": "The existing acceptance question says `--json` must be supported, but the repo's JSON commands usually expose a stable schema field and predictable command-specific fields. Without this, tests may only prove that some JSON exists rather than the contract users can rely on.",
        "recommended_answer": "Return a `pactum.export.v1` JSON object containing at least `schema`, `run_id`, `output`, `archive_format`, `archive_root`, `entries`, `bytes`, and `filtered_events`. The human output should report the same essentials in text form without printing archive contents.",
        "confidence": "high",
        "status": "answered",
        "answer": "Return a `pactum.export.v1` JSON object containing at least `schema`, `run_id`, `output`, `archive_format`, `archive_root`, `entries`, `bytes`, and `filtered_events`. The human output should report the same essentials in text form without printing archive contents."
      }
    ]
  },
  "memory_context": {
    "used_items": []
  }
}
